apiVersion: v1
kind: Service
metadata:
  name: admin-backend
  namespace: saviaup-qa
  labels:
    app.kubernetes.io/name: admin-backend
    app.kubernetes.io/part-of: saviaup
    environment: qa
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: admin-backend
    app.kubernetes.io/part-of: saviaup
    environment: qa
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
