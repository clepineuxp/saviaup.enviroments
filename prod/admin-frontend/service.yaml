apiVersion: v1
kind: Service
metadata:
  name: admin-frontend
  namespace: saviaup-prod
  labels:
    app.kubernetes.io/name: admin-frontend
    app.kubernetes.io/part-of: saviaup
    environment: prod
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: admin-frontend
    app.kubernetes.io/part-of: saviaup
    environment: prod
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
