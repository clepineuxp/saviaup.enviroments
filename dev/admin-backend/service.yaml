apiVersion: v1
kind: Service
metadata:
  name: admin-backend
  namespace: saviaup-dev
  labels:
    app.kubernetes.io/name: admin-backend
    app.kubernetes.io/part-of: saviaup
    environment: dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: admin-backend
    app.kubernetes.io/part-of: saviaup
    environment: dev
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
