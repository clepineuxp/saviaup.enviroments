apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: saviaup-dev
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: saviaup
    environment: dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: saviaup
    environment: dev
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
