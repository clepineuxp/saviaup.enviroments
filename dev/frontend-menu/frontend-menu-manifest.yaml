apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-menu-config
  namespace: saviaup-dev
  labels:
    app.kubernetes.io/name: frontend-menu
    app.kubernetes.io/part-of: saviaup
    environment: dev
data:
  API_URL: "https://dev-api.saviaup.com"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-menu
  namespace: saviaup-dev
  labels:
    app.kubernetes.io/name: frontend-menu
    app.kubernetes.io/part-of: saviaup
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend-menu
      app.kubernetes.io/part-of: saviaup
      environment: dev
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend-menu
        app.kubernetes.io/part-of: saviaup
        environment: dev
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: frontend-menu
          image: ghcr.io/clepineuxp/saviaup-frontend-menu:dev
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          envFrom:
            - configMapRef:
                name: frontend-menu-config
            - secretRef:
                name: frontend-menu-secret
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-menu
  namespace: saviaup-dev
  labels:
    app.kubernetes.io/name: frontend-menu
    app.kubernetes.io/part-of: saviaup
    environment: dev
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend-menu
    app.kubernetes.io/part-of: saviaup
    environment: dev
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
